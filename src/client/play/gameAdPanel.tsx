import * as React from 'react';

// Ads removed in the remaster (the original project's license only permits
// free, non-commercial use). This panel renders nothing.
export class GameAdPanel extends React.PureComponent {
    render() {
        return null;
    }
}

export default GameAdPanel;

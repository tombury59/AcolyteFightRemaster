import * as React from 'react';

// Social links removed in the remaster (private gameplay between friends).
export class SocialBar extends React.PureComponent {
    render() {
        return null;
    }
}

export default SocialBar;
